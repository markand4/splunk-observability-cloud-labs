version: "3.9"

services:
  websocket-app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ../../.env
    environment:
      - OTEL_SERVICE_NAME=fastapi-websocket-demo
      - OTEL_ENVIRONMENT=demo
    restart: unless-stopped
